<%= form_for(@score) do |f| %>
  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rider_id %><br />
    <%= f.select :rider_id, options_from_collection_for_select(Rider.all, "id", "full_name"), :include_blank => (action_name == "new")  %>
  </div>
  <div class="field">
    <%= f.label :circuit_id %><br />
    <%= f.select :circuit_id, options_from_collection_for_select(Circuit.all, "id", "name") %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.text_field :position %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.text_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
